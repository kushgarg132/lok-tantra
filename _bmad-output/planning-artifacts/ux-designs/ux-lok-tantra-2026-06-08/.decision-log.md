# Decision Log — Power Explorer UX

Canonical memory and audit trail for this UX run. Every decision, change, and override is recorded here as it happens.

## 2026-06-08

- **Intent**: Create (no prior UX run found under `_bmad-output/planning-artifacts/ux-designs/`).
- **Source**: `bmad-prd` PRD for "Power Explorer" — `_bmad-output/planning-artifacts/prds/prd-lok-tantra-2026-06-08/prd.md` (status: final). UX follows directly from this PRD; inherits its Glossary, UJs, and §5 Neutrality guardrails by reference rather than duplicating them.
- **Stakes**: Demo — user said "Its for demo as of now." Lighter rigor than the PRD's launch-grade calibration; spines should be complete and coherent but need not exhaustively cover edge cases or polish to production depth.
- **Working mode**: Fast path — user said "Fast." PM batches remaining gaps into consolidated questions, drafts both spines directly with `[ASSUMPTION]` tags, and skips creative tools (no color-theme/wireframe/key-screen exploration before drafting).
- **Visual direction — locked via consolidated AskUserQuestion**: "Extend the existing theme" (chosen over a standalone identity). Power Explorer inherits LokTantra's existing Tailwind-based saffron/navy/chakra palette, `class`-strategy dark mode, and Inter/Plus Jakarta Sans/JetBrains Mono font stack — DESIGN.md specifies deltas (how these tokens apply to graph-specific concerns: party-color neutral palette, node/edge states, Time Travel chrome) rather than inventing a new system. Grounded in `tailwind.config.ts` (read in full — exact hex values, font families, animation keyframes extracted; no custom `borderRadius`/`spacing`/`boxShadow` extensions exist, so DESIGN.md inherits Tailwind defaults for those).
- **Experience mood — locked via the same consolidated question**: "Cinematic & immersive." Graph transitions read as camera movement through connected space; Time Travel scrubbing feels like rewinding through history; motion carries real emotional weight (within the "demo" stakes — polished enough to *feel* the intended direction, not production-hardened). This directly shapes DESIGN.md's Elevation & Depth / motion language and EXPERIENCE.md's Interaction Primitives.
- **Both spines drafted in full (Fast path)** — `DESIGN.md` and `EXPERIENCE.md` now carry complete content (frontmatter tokens + all 8 locked-order body sections each, plus an invented "Responsive & Platform" section in EXPERIENCE.md; "Inspiration & Anti-patterns" deliberately omitted — no inspirations were ever elicited from the user, and inventing them would cross from capturing into authoring). Grounding used: `tailwind.config.ts` (read in full — saffron/navy/chakra ramps, Inter/Plus Jakarta Sans/JetBrains Mono, existing `fade-in`/`slide-up`/`slide-in-right` keyframes, no custom rounded/spacing/shadow scales), the `design-example-mobile.md` and `experience-example-mobile.md` "Quill" worked examples (shape/depth/voice templates), and the finalized PRD (UJs mirrored verbatim, FR-7's three Office-states, §5 Neutrality guardrails, Glossary terms used exactly).
  - **Key invented deltas** (this UX run's actual creative contribution, since "extend the existing theme" meant the base palette/type was already fixed): a six-color muted, equal-weight **party-neutral tag palette** (`party-tag-1`..`6`, placeholder hex — flagged `[ASSUMPTION]` as the single highest reputational-risk color decision, needs sign-off before real build); a **Person = circle / Office = rounded-square** shape grammar making "the seat vs. its occupant" visible independent of color (operationalizes §5's "Office, not officeholder" framing); two new named motions, `graph-traverse` and `time-rewind`, proposed to extend `tailwind.config.ts`'s animation set (cinematic camera-movement + history-rewind feel — UJ-2's emotional payload); a single-canvas-plus-overlays IA (no tabs/drawer — the graph is the permanent home, everything else is a dismissible layer over it).
  - **`[ASSUMPTION]` tags placed** (to be triaged at Finalize): dark mode as the graph canvas's default register; press-and-hold reserved/unused on Nodes; cold-open skeleton/shimmer treatment; search empty-state copy; offline/fetch-failure treatment (flagged as open for architecture — PRD is silent here); tap-target-vs-zoom trade-off at deep zoom-out; large-viewport (tablet/desktop) panel-vs-sheet treatment; the "cinematic & immersive" mood interpretation itself (depth + camera-like motion vs. alternative readings).
  - Both files remain `status: draft` — Finalize (spines distilled/reviewed/triaged/polished/closed) is the remaining work.
- **Spines distilled — self-run Pass 1 coverage check** (rubric walker categories 1–3, run proactively before offering the Reviewer Gate, per the skill's instruction): caught and fixed three classes of gap directly —
  1. **Broken `{}` cross-references**: an in-place find/replace pass to normalize `tailwind.config.ts` references accidentally stripped several inline references down to empty braces (`` `{}` ``); restored all of them to plain code-style `` `tailwind.config.ts` `` / `` `tailwind.config.ts (theme.extend.X)` `` mentions — these are *external-file* references, not `{path.to.token}` cross-refs, so brace syntax was the wrong notation for them in the first place.
  2. **Token-path qualification**: DESIGN.md mixed bare `{headline}`/`{body}`/`{data}` with fully-qualified `{typography.headline}`/`{typography.body}`/`{typography.data}` for the same nested tokens — standardized on the fully-qualified form throughout (frontmatter `components:` block, the Components markdown table). Also corrected the `typography:` frontmatter values themselves, which referenced bare `{display}`/`{sans}`/`{mono}` as if they were self-referencing tokens (they're inherited Tailwind font-family *names*, not this file's tokens) — now stated as plain `font-display`/`font-sans`/`font-mono` mentions of the inherited stack.
  3. **Cross-spine reference notation**: EXPERIENCE.md referenced DESIGN.md's proposed `graph-traverse`/`time-rewind` motions using `{}` cross-ref syntax, implying they resolve as DESIGN.md frontmatter tokens — they don't (DESIGN.md has no `motion:` token block; they're named concepts proposed in its Elevation & Depth prose, both `[ASSUMPTION]`-tagged as proposals for engineering to formalize). Switched to plain backtick code-style references in EXPERIENCE.md, matching how DESIGN.md itself names them.
  - **Flow coverage**: added a failure/sparse-record path to UJ-2's Key Flow (it had a climax but no failure path, unlike UJ-1) — grounded directly in FR-7's "approximate date" convention, so Priya's 1947 view is honestly thin rather than silently wrong.
  - No remaining broken cross-references found on re-grep of both files.
- **Reviewer Gate — user picked "Rubric walker + Neutrality + Accessibility"** (declined the offered "Skip" and a separate motion/performance lens was not offered as its own option): three lenses dispatched as parallel subagents against the drafted `DESIGN.md` + `EXPERIENCE.md` spine pair. Each writes full findings to `review-{slug}.md` and returns a compact summary only (per the skill's context-protection convention). Results synthesized below once all three return.
- **Input reconciliation skipped**: `imports/` and `.working/` are both empty — no user-supplied visuals, sketches, brand decks, or other source documents exist for this UX run beyond the PRD itself (already inherited by reference in both spines' `sources:` frontmatter, not a reconciliation target). Mirrors the same call made at the PRD's Finalize.
- **Reviewer Gate — results returned (3 critical / 9 high / 11 medium / 7 low across the three lenses)**:
  - **Rubric walker** (`review-rubric.md`, 1 critical/3 high/6 medium/5 low): verdict = strong Fast-path draft, not yet a clean source-extraction contract. Top findings: `state-occupied`/`state-not-established` ship as descriptive prose with no resolvable hex (critical); eleven `{}`-wrapped cross-references in DESIGN.md's frontmatter point at `navy.*`/`saffron.*`/`chakra.*` paths that don't exist in this file's own token tree — an external-system value wrapped in internal cross-ref syntax (high); `Search` has a full EXPERIENCE.md behavioral row but no DESIGN.md visual-spec row at all (high); EXPERIENCE.md's `{time-marker-approximate}` ref only resolves under the same bare-key convention DESIGN.md uses inconsistently — a smaller, cross-spine echo of the same break (high).
  - **Neutrality reviewer** (`review-neutrality.md`, 1 critical/3 high/3 medium/1 low): verdict = real operationalization effort, but assertion outpaces enforced mechanism on three of four §5 rules. Top findings: `party-tag-2` (`#A8856B`, hue 26°) is measurably a desaturated saffron — colliding with both the Person register and BJP's brand color, directly contradicting the file's own "none overlaps saffron" claim (critical); no rule states what the cinematic camera focuses on *during* `time-rewind` — leaves open the editorially-loaded possibility that the "Focused" treatment follows historical officeholders' faces through a 75-year scrub rather than staying anchored to the Office (high); "context travels with content" is wired to the Accountability block and citation panel but never to the actual surface UJ-1's climax says gets screenshotted — the expanded Node/profile header (high); six party-tag colors is very likely short of what a national-layer coalition cabinet needs on screen at once, and no overflow/assignment rule exists for color #7 (high).
  - **Accessibility reviewer** (`review-accessibility.md`, 1 critical/3 high/2 medium/1 low): verdict = meaningfully better than baseline, but stalls at "principle" on the two hardest cases. Top findings: EXPERIENCE.md specifies how a single Node announces itself to a screen reader but never how a screen-reader user moves *between* connected Nodes — the core interaction model is undefined for that population (critical); `state-vacant`/`edge-base` measure under the WCAG 3:1 non-text-contrast floor against the light-mode canvas — the *entire visual signal* for "Office is vacant" nearly disappears (high); five of six `party-tag-*` placeholders also fail contrast, and the spec doesn't say whether the color is the chip's fill or an accent — two builders would ship two different, differently-accessible components from the same line (high); the tap-disabled-until-pinch-zoom fallback (carried verbatim in both files as an `[ASSUMPTION]`) trades a small-tap-target problem for a worse one — pinch-to-zoom is itself inaccessible to switch-access/single-finger users, who'd be left with zero path to the Node (high).
  - **Convergence**: all three lenses independently flagged the placeholder `party-tag-*` palette — from three different angles (brand-collision, perceptual-equality, WCAG contrast) — making it the single most load-bearing fix in the open-items triage that follows.
  - Synthesis presented to user as a tiered verdict (8 thematic clusters covering all 3 critical + 9 high findings); medium/low rolled into a tail pointer to the three `review-*.md` files.
- **Reviewer findings — first resolution pass (applied to files, not previously logged)**: the following critical/high findings were resolved directly in the spines:
  1. Party-tag palette replaced: hues shifted to violet→magenta→rose arc (250°–330°), all six at identical 30% saturation / 48% lightness, 16° hue intervals. Resolves Neutrality #1 (critical), #6 (medium), Accessibility #2 (high, contrast + ambiguity).
  2. Focused anchor rule locked for `time-rewind`: "the Focused anchor remains the Office… never the occupant" — added as an explicit rule in DESIGN.md Elevation & Depth. Resolves Neutrality #2 (high).
  3. Provenance line added to `node-card-person` and `node-card-office` components: persistent `{typography.data}` line ("Power Graph · [date] · Source: [source]") rendered inside the card's frame. Resolves Neutrality #3 (high).
  4. Party-tag overflow rule added: alphabetical assignment order + `party-tag-overflow` neutral gray for parties beyond slot 6. Resolves Neutrality #4 (high).
  5. Contrast fixes: `edge-base`/`edge-base-dark` corrected (light/dark swapped + values adjusted to clear 3:1); `state-vacant` given explicit light/dark pair; `scrubber-track` corrected; `time-marker-approximate` shifted to warm stone-gray off saffron ramp. Resolves Accessibility #1 (high), #6 (low), Neutrality #8 (low).
  6. Party-tag-chip fill-vs-accent resolved: dot-only accent + neutral surface fill + standard ink text. Resolves Accessibility #2 (high, ambiguity half).
  7. Motion budget thresholds added: ~400ms ceiling, light-defocus cap, ±15% parallax delta. Resolves Accessibility #5 (medium).
  8. Frontmatter cross-refs fixed: `{navy.*}`/`{saffron.*}`/`{chakra.*}` replaced with literal hex + comments; `state-occupied`/`state-not-established` restructured as `{opacity, outline}` modifier objects; bare color refs qualified as `{colors.*}`. Resolves Rubric #1 (critical), #2 (high), #3 (medium), #4 (low).
- **Reviewer findings — second resolution pass (session 2, 2026-06-08)**: user chose "resolve all" for the 13 remaining open items. All resolved:
  1. **Screen-reader graph traversal (Accessibility #3, critical)**: added connections-list contract to EXPERIENCE.md Accessibility Floor — from any focused Node, connected Nodes are linearly reachable via standard next/previous gestures in hierarchy-rank order; activating an item triggers the same `graph-traverse` sighted users get. DOM structure flagged as `[ASSUMPTION — open for architecture]`.
  2. **Tap-disabled-until-pinch-zoom regression (Accessibility #4, high)**: updated the `[ASSUMPTION]` in both files — tap-disabled Nodes remain reachable via Search and the connections list; pinch-to-zoom is one path in, never the only path.
  3. **Search missing from DESIGN.md (Rubric, high)**: added `search-overlay` to frontmatter `components:` and Components table with visual spec (full-bleed overlay, focus ring, compact node-card result rows).
  4. **Profile sheet missing from DESIGN.md (Rubric, medium)**: added `profile-sheet` to frontmatter `components:` and Components table with visual spec (bottom-sheet, fixed-order sections, provenance footer).
  5. **party-tag-chip / state-badge missing from EXPERIENCE.md (Rubric, medium)**: added both rows to Component Patterns — party-tag-chip: non-interactive, identical rendering; state-badge: synchronous re-render on `time-rewind`, identical across surfaces.
  6. **Profile sheet / citation panel loading states (Rubric, medium)**: added "Profile sheet loading" and "Profile / citation fetch failure" rows to State Patterns.
  7. **Search idle / pending states (Rubric, medium)**: added "Search — idle" and "Search — pending" rows to State Patterns.
  8. **Flow 1 directional narration (Neutrality #5, medium)**: reworded step 4 — "a traced line from state legislator to Union Minister" → "a chronological list of her prior Tenures — Office, dates, and how each transition occurred (elected / appointed / reshuffled) — up to her current position."
  9. **Comparative symmetry enforcement (Neutrality #7, medium)**: added "no escape hatch" sentence to `accountability-block` in both frontmatter and Components table — "takes only data as input; no props, flags, or content-authoring path can change fields per-Person."
  10. **Focus-return ambiguity (Accessibility #7, medium)**: extended the Accessibility Floor rule — focus returns to the Node's current post-`graph-traverse` position, not a cached pre-navigation reference.
  11. **edge-base orphaned in prose (Rubric, low)**: added `{colors.edge-base}` / `{colors.edge-base-dark}` stroke reference to Shapes → Edges.
  12. **Node naming mismatch (Rubric, low)**: added visual-spec cross-reference to EXPERIENCE.md's Node row — "visual: DESIGN.md → node-card-person (circle) and node-card-office (rounded square); Institution shares Office's card template."
  13. **Scrub-in-flight state (Rubric, low)**: added "Scrub in progress" row to State Patterns with `[ASSUMPTION — pending Open Question #1]` flag.
  - **All 30 original findings (3 critical + 9 high + 11 medium + 7 low) are now resolved.** No open reviewer findings remain.

## 2026-06-09

- **Open items triaged**: 14 `[ASSUMPTION]` tags remain across both spines — all classified as non-blockers at demo stakes. Carry to architecture/build; none gate finalization.
- **Key-screen mocks skipped** (user choice) — all IA surfaces will be built from spine tables alone.
- **Polish passes applied**: editorial-review-structure and editorial-review-prose run inline. One fix: stray `{saffron.300}` external cross-ref in DESIGN.md → Colors converted to backtick mention (`saffron.300`). No other structural or prose issues found.
- **Both spines set to `status: final`, `updated: 2026-06-09`.** Finalization complete.
- **Common next steps**: `bmad-create-architecture` (system design for the Power Explorer feature), `bmad-create-epics-and-stories` (break the spines into buildable stories), `bmad-dev-story` (pick a story and start building).
